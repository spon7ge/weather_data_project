weather_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'weather_postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'weather') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'weather') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'weather_db') }}"
      schema: analytics
      threads: 4
